---

- name: Get IP addresses from IPAM, add hosts to Tower, push to repo
  hosts: ipam
  connection: local
  gather_facts: no

  
  roles:
    - manage-secret
    - getIPsAllBigips
    - buildHostsOnTower
    - buildSOTF5Tenant

